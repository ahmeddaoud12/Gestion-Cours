@{
    @if (User.Identity.IsAuthenticated & User.IsInRole("Admin"))
    {
        Layout = "_AdminLayout";
    }
    else if (User.Identity.IsAuthenticated & User.IsInRole("Etudiant"))
    {
        Layout = "_Layout";
    }
    else
    {
        Layout = "_LayoutVisitor";
    }
}
